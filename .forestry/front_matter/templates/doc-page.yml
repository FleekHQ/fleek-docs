---
label: doc-page
hide_body: false
fields:
- name: template
  type: text
  config:
    required: false
  description: This is just to fill the frontmatter and prevent a bug with the framework
  label: template
  default: doc
  hidden: true
